// Kybernesis Engineer: vision-verified dev loop, trade-school skills, and
// ship discipline. Pairs with the workshop sandbox (agent/sandbox/sandbox.ts,
// installed by the same registry item) — Playwright bakes into the sandbox
// TEMPLATE on first use, so later sessions start warm.
export { default } from "@kybernesis/engineer";
