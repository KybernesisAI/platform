import { defineExtension } from "eve/extension";

/**
 * Kybernesis Engineer: the capability layer that turns an eve agent into a
 * working software engineer — a vision-verified dev loop (in-sandbox
 * screenshots the model can actually see), trade-school skills for
 * scaffolding, iterating, verifying, committing, and shipping, and the
 * discipline rules that keep production sacred.
 *
 * Pairs with the "workshop" sandbox template (Playwright baked into the
 * template, warm sessions) that the Kybernesis registry item or
 * `kyb init --engineer` writes — sandbox definitions are consumer-authored
 * and cannot ship inside an extension.
 */
export default defineExtension();
